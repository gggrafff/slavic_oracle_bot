# Slavic Oracle Bot

**Slavic Oracle Bot** — это Telegram-бот для работы с метафорическими картами, основанными на славянской мифологии. Бот позволяет пользователям получать предсказания и читать толкования.

## 🛠 Технологии

*   **Python**: 3.12+
*   **Telegram API**: [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) (v21.10+)
*   **Управление зависимостями**: [Poetry](https://python-poetry.org/)
*   **Тестирование**: [pytest](https://docs.pytest.org/)

## 🚀 Установка и настройка

### Предварительные требования

Убедитесь, что у вас установлены:
1.  Python 3.12 или выше.
2.  Poetry.

### Установка зависимостей

1.  Клонируйте репозиторий:
    ```bash
    git clone <URL_репозитория>
    cd SlavicOracleBot
    ```

2.  Установите зависимости через Poetry:
    ```bash
    poetry install
    ```

## ▶️ Запуск

Для запуска бота требуется токен, который можно получить у [@BotFather](https://t.me/BotFather).

### Локальный запуск

Используйте команду `poetry run` для запуска скрипта `main.py`, передав токен в качестве аргумента:

```bash
poetry run python main.py <ВАШ_TELEGRAM_TOKEN>
```

Пример:
```bash
poetry run python main.py 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

### Запуск через Docker

1.  Соберите образ:
    ```bash
    docker build -t slavic-oracle-bot .
    ```

2.  Запустите контейнер (передав токен как аргумент команды):
    ```bash
    docker run -d --name slavic-oracle slavic-oracle-bot python main.py <ВАШ_TELEGRAM_TOKEN>
    ```

## 🧪 Тестирование

Проект покрыт unit-тестами (pytest). Тесты проверяют чтение карт, логику меню и формирование сообщений.

Для запуска тестов выполните:
```bash
poetry run pytest -v
```

## 📂 Структура проекта

*   `main.py` — Точка входа в приложение.
*   `bot/` — Основная логика бота (меню, локации, обработчики).
*   `cards/` — Данные карт (CSV с описаниями и папка `images/` с изображениями).
*   `tests/` — Тесты.
*   `utils.py` — Вспомогательные утилиты.
